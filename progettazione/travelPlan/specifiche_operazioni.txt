SPECIFICHE OPERAZIONI 

1. Dato un viaggio e un utente che vi partecipa, calcolare il numero di attivit`a del viaggio
di tipo “spostamento” alle quali ha preso parte

prima_operazione(v: Viaggio, u:Utente) -> Intero:

    pre-condizioni:
        -deve esservi almeno un link di associazione tra viaggio e utente tale che (u, v) partecipa, senno result = 0

    post-condizioni:
        -sia E l'insieme degli oggetti s: Spostamento che partecipano a dei link di associazione (s, v) Associato
        -sia B l'insieme dei link di associazione (s, u) ut_att in E
        -sia N la cardinalità
        -result = N


2. Dato un viaggio, ottenere gli utenti che hanno partecipato al viaggio e che hanno preso
parte al maggior numero di attivit`a organizzate in esso.

seconda_operazione(v: Viaggio) -> Intero:

    pre-condizioni:
        -deve esservi almeno un link di associazione tra utente e viaggio 

    post_condizioni:
        -sia E l'insieme di oggetti u:Utenti che partecipano dei link di associazione (u, v) partecipa e (u,v) organizza 
        -per ogni u: Utente nell'insieme E sia B l'insieme dei link di associazione (u, a) ut_att
        -sia result l'oggetto u: Utente con cardinalità maggiore


3. I responsabili del sistema, data una nazione, devono poter calcolare, per ognuna delle
sue regioni, la/e citt`a toccata/e dal maggior numero di viaggi.

terza_operazione(n: Nazione) -> intero:

    post_condizioni:
        -sia NR l'insieme dei link di associazione tali che (r: Regione, n): reg_naz
        -per ogni r: Regione sia CR l'insieme dei link di associazione tali che (c: Città, r) cit_reg appartenente a NR
        -per ogni c: città sia LC l'insieme dei link di associazione tali che (c, l: Luogo) l_cit appartenente a CR
        -per ogni 


4. Data un’attivit`a, calcolare il numero di utenti che vi ha preso parte.

quarta_operazione(a: Attività) -> Intero:

    -pre_condizioni:
        -deve esistere almeno un link di associazione tra attività e utente 

    -post_condizioni:
        -sia E l'insieme degli oggetti u:Utente che partecipano ai link di associazione (a, u) ut_att 
        -sia N la cardinalità di E
        -result = N 


5. Dato un viaggio, ottenere la/e attivit`a organizzata/e in esso alla/e quale/i ha preso
parte il maggior numero di utenti.

quinta_operazione(v: Viaggio) -> Attività:

    -pre_condizioni:

    -post_condizioni:
        -sia E l'insieme di oggetti a: Attività che partecipano a un link di associazione (a, v) Associato
        -per ogni oggetto a nell'insieme E quarta_operazione(a)
        -sia result l'oggetto a con il valore di quarta_operazione(a) massimo


6. Data una citt`a di destinazione, per ognuno dei 12 mesi dell’anno, calcolare il numero
di viaggi organizzati in tale mese nell’ultimo anno solare, in tale citt`a

    sesta_operazione(c: Città)

    -pre_condizioni:


    -post_condizioni:
        -sia X l'insime dei link di associazione tra c: Città e o: Organizzatore 



